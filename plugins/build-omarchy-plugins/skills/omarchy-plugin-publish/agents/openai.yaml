interface:
  display_name: "Publish Omarchy Plugins"
  short_description: "Prepare exact marketplace submissions and updates"
  default_prompt: "Use $omarchy-plugin-publish to prepare this plugin's marketplace submission for my approval."
