interface:
  display_name: "Migrate Omarchy Plugins"
  short_description: "Move legacy integrations safely onto Quattro"
  default_prompt: "Use $omarchy-plugin-migrate to separate this legacy integration into a safe Quattro shell layer and machine layer."
