# Release notes — 0.2.1

Adds explicit OpenCode support to the provider-neutral Agent Plugins 1.0.0
distribution. The same twelve skills now install safely into OpenCode's native
project and global skill directories, with shared `.agents/skills` discovery
remaining available. The OpenAI skills-only adapter is unchanged apart from the
aligned release version.
